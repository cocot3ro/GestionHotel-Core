name: Build and Release GestionHotel-Core Application

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types:
      - closed
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-latest
    
    # if: github.event.pull_request.merged == true

    strategy:
      matrix:
        package_type: [deb, rpm]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Java 21
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '21'

    - name: Build with Maven
      working-directory: ./GestionHotel-Core
      run: |
        mvn clean package
        mv target/GestionHotel-Core*.jar target/GestionHotel-Core.jar

    - name: Generate DEB & RPM Installers
      working-directory: ./GestionHotel-Core
      run: |
        jpackage --type ${{ matrix.package_type }} \
        --input target/ \
        --main-jar GestionHotel-Core.jar \
        --main-class com.cocot3ro.gestionhotel.core.GestionHotelCoreApplication \
        --name "GestionHotel Core" \
        --dest target/installer

    - name: Upload DEB & RPM Installers
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.package_type }}-installer
        path: GestionHotel-Core/target/installer/*.${{ matrix.package_type }}

  build-windows:
    runs-on: windows-latest

    # if: github.event.pull_request.merged == true

    strategy:
      matrix:
        package_type: [msi, exe]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Java 21
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '21'

    - name: Build with Maven
      working-directory: ./GestionHotel-Core
      run: |
        mvn clean package
        mv target/GestionHotel-Core*.jar target/GestionHotel-Core.jar

    - name: Generate MSI & EXE Installers
      working-directory: ./GestionHotel-Core
      run: |
        jpackage --type ${{ matrix.package_type }} --input target/ --main-jar GestionHotel-Core.jar --main-class com.cocot3ro.gestionhotel.core.GestionHotelCoreApplication --name "GestionHotel Core" --dest target/installer

    - name: Upload MSI & EXE Installers
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.package_type }}-installer
        path: GestionHotel-Core/target/installer/*.${{ matrix.package_type }}

  release:
    needs: 
      - build-linux
      - build-windows
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Java 21
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '21'

    - name: Get project version
      id: get-version
      working-directory: GestionHotel-Core
      run: echo "VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)" >> $GITHUB_ENV

    - name: Download Artifacts
      uses: actions/download-artifact@v4

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        name: v${{ env.VERSION }}
        tag_name: v${{ env.VERSION }}
        draft: false
        prerelease: false
        files: |
          ./msi-installer/*.msi
          ./exe-installer/*.exe
          ./deb-installer/*.deb
          ./rpm-installer/*.rpm

    # - name: Create Release
    #   id: create_release
    #   uses: actions/create-release@v1
    #   env:
    #       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     tag_name: v${{ env.VERSION }}
    #     release_name: v${{ env.VERSION }}
    #     draft: false
    #     prerelease: false
    #     body: "Release of version ${{ env.VERSION }}"

    # - name: Download MSI Installer Artifact
    #   uses: actions/download-artifact@v4
    #   with:
    #     name: msi-installer
    #     path: ./msi-installer

    # - name: Upload MSI Installer to Release
    #   uses: actions/upload-release-asset@v1
    #   env:
    #       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     upload_url: ${{ steps.create_release.outputs.upload_url }}
    #     asset_path: ./msi-installer/*.msi
    #     asset_name: GestionHotel-Core-v${{ env.VERSION }}.msi
    #     asset_content_type: application/x-msi

    # - name: Download EXE Installer Artifact
    #   uses: actions/download-artifact@v4
    #   with:
    #     name: exe-installer
    #     path: ./exe-installer

    # - name: Upload EXE Installer to Release
    #   uses: actions/upload-release-asset@v1
    #   env:
    #       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     upload_url: ${{ steps.create_release.outputs.upload_url }}
    #     asset_path: ./exe-installer/*.exe
    #     asset_name: GestionHotel-Core-v${{ env.VERSION }}.exe
    #     asset_content_type: application/x-msdownload

    # - name: Download DEB Installer Artifact
    #   uses: actions/download-artifact@v4
    #   with:
    #     name: deb-installer
    #     path: ./deb-installer

    # - name: Upload DEB Installer to Release
    #   uses: actions/upload-release-asset@v1
    #   env:
    #       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     upload_url: ${{ steps.create_release.outputs.upload_url }}
    #     asset_path: ./deb-installer/*.deb
    #     asset_name: GestionHotel-Core-v${{ env.VERSION }}.deb
    #     asset_content_type: application/vnd.debian.binary-package

    # - name: Download RPM Installer Artifact
    #   uses: actions/download-artifact@v4
    #   with:
    #     name: rpm-installer
    #     path: ./rpm-installer

    # - name: Upload RPM Installer to Release
    #   uses: actions/upload-release-asset@v1
    #   env:
    #       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     upload_url: ${{ steps.create_release.outputs.upload_url }}
    #     asset_path: ./rpm-installer/*.rpm
    #     asset_name: GestionHotel-Core-v${{ env.VERSION }}.rpm
    #     asset_content_type: application/x-rpm