server:
  port: 8080

spring:
  application:
    name: msvc-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: msvc-almacen
          uri: http://localhost:8980
          predicates:
            - Path=/api/almacen/**